[project]
name = "heart-disease-analysis"
version = "1.0.0"
description = "Flask and Tableau-ready heart disease analytics application."
requires-python = ">=3.12,<3.13"
dependencies = [
    "Flask==3.0.3",
    "Werkzeug==3.0.3",
    "Authlib==1.3.2",
]

[tool.uv]
package = false
